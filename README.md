# weather-monitor

Собирает погоду, космическую погоду (Kp/солнечный ветер/аврора/резонанс Шумана), природные катаклизмы (NASA EONET, USGS), «цифровой шум» (частота публикаций в новостных RSS-лентах) и рыночную волатильность (5 базовых индексов/валют) — считает интегральный индекс активности и рендерит сайт из 4 вкладок. По расписанию (GitHub Actions, раз в день) проверяет, всё ли спокойно, и если нет — шлёт уведомление в Telegram.

Отдельно, независимо от основного индекса — **индекс опасности для Шри-Ланки**: те же самые фиды EONET/USGS, гео-отфильтрованные по радиусу вокруг острова (землетрясения — 2500 км и не старше `SRI_LANKA_QUAKE_MAX_AGE_DAYS` = 3 дней, штормы — 1500 км), с собственным взвешенным скорингом (`compute_sri_lanka_index` в `composite.py`). Возрастной фильтр на землетрясения добавлен 2026-09-08 — без него старое событие держало индекс на одном и том же значении неделями, пока не выпадало из 30-дневного окна USGS (см. `SRI_LANKA_QUAKE_MAX_AGE_DAYS` в `sources.py`). Всегда виден в уведомлении отдельным блоком, не только при срабатывании триггера, вместе с температурой воды/воздуха и скоростью ветра для Коломбо (`fetch_sri_lanka_weather`/`fetch_sri_lanka_marine` — Open-Meteo, бесключевой источник, координаты `SRI_LANKA_COASTAL_LAT/LON` намеренно отличаются от `SRI_LANKA_LAT/LON` выше — та пара это географический центр острова для геопоиска, не прибрежная точка).

Дизайн вдохновлён sungeo.net — earth-tone палитра, кольцевой gauge, статус-бейджи.

## Локально

```
pip install -r requirements.txt
export OPENWEATHERMAP_API_KEY=...   # openweathermap.org/api, бесплатный тариф
python build.py "Pekin,CH"           # или любой другой город
```

Открой `index.html` в браузере.

## Источники данных (все без ключа, кроме OpenWeatherMap)

| Источник | Что даёт |
|---|---|
| OpenWeatherMap | текущая погода + прогноз |
| NOAA SWPC | Kp/Ap-индекс, солнечный ветер, аврора |
| Schumann Resonance Live | резонанс Шумана (неофициальный источник, честно помечается, когда данные не свежие) |
| NASA EONET | открытые природные события (штормы — источник для индекса Шри-Ланки) |
| USGS | значимые землетрясения за месяц, отфильтрованные до последних 3 дней (источник для индекса Шри-Ланки) |
| Open-Meteo / Open-Meteo Marine | температура воздуха, ветер, температура воды у Коломбо |
| BBC / Google News / Al Jazeera / RIA Novosti (RSS) | «цифровой шум» — частота публикаций как косвенный индикатор |
| Yahoo Finance chart API | S&amp;P 500, VIX, MOEX, USD/RUB, EUR/USD — дневное % изменение |

## Как устроена автоматизация (`.github/workflows/daily-check.yml`)

Этот репозиторий публичный и хранит только код — ни город, ни история запусков сюда не коммитятся (см. `.gitignore` и docstring в `build.py`/`history_sync.py`): публичная git-история, растущая по расписанию, тихо палила бы геолокацию и паттерн активности. Поэтому:

1. **История** (`data/history.jsonl`) синхронизируется с отдельным приватным репозиторием через `history_sync.py` (pull перед сборкой, push после) — сюда, в публичный репо, она не пишется.
2. **Сгенерированные HTML-страницы** нигде не коммитятся — это просто разовый вывод одного прогона, не персистентный сайт.
3. **Условие алерта** (`notify.py`): по умолчанию (`NOTIFY_MODE=always`, settled-режим) уведомление в Telegram уходит при каждом прогоне независимо от триггера — блок Шри-Ланки виден в нём всегда. Если `NOTIFY_MODE=condition` выставлен явно, отправка только когда сработал триггер: интегральный индекс ≠ «Спокойно», ИЛИ рыночная волатильность/новостной шум заметно (&gt;1.5×) выше исторического среднего (только когда истории накопилось достаточно), ИЛИ индекс Шри-Ланки ≠ «Спокойно» — землетрясение с магнитудой ~M5.75+ в радиусе 2500 км за последние 3 дня, или активный шторм в бассейне.

### Секреты репозитория (Settings → Secrets and variables → Actions)

| Секрет | Назначение |
|---|---|
| `OPENWEATHERMAP_API_KEY` | ключ погоды |
| `WEATHER_CITY` | город, например `Pekin,CH` — не хранится в коде |
| `HISTORY_PUSH_TOKEN` | GitHub PAT с правом записи в приватный репо истории |
| `TELEGRAM_MOTION_BOT_TOKEN` | токен Telegram-бота для алертов |
| `TELEGRAM_MOTION_CHAT_ID` | куда слать алерт |
| `NOTIFY_MODE` | необязательный; `always` (по умолчанию, если не задан) или `condition` — см. раздел выше |

## Файлы

- `sources.py` — сбор данных со всех источников
- `moon.py` — фаза Луны, чистый расчёт, без API
- `composite.py` — интегральный индекс активности (нормализация + взвешенный блендинг) + отдельный индекс опасности для Шри-Ланки
- `charts.py` — SVG line/bar/ring-графики
- `templates.py` — общий каркас страницы (навигация, CSS)
- `pages.py` — тело каждой вкладки
- `build.py` — точка входа: собирает данные, считает индекс, пишет историю + сводку, рендерит 4 HTML-файла
- `history_sync.py` — pull/push `data/history.jsonl` из/в приватный репозиторий
- `notify.py` — проверяет условие алерта, шлёт в Telegram
- `.github/workflows/daily-check.yml` — расписание + шаги
